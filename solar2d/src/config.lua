-- TODO:
-- Make a dynamic resolution/content area selection based on content area from the web GUI.

application = {
    content = {
        width = 640,
        height = 960,
        scale = "letterbox",
        fps = 60,
    },
}
